enum Role {
  USER
  ADMIN
  ROOT
}

enum PaymentMethod {
  TRANSFERENCIA
  CARGO_BANCARIO
}

enum TaskType {
  IMPORTANTE
  URGENTE
  PROPUESTA
}

enum TaskStatus {
  CREADA
  ASIGNADA
  EN_CURSO
  RESUELTA
  ESPERANDO_VALIDACION
}